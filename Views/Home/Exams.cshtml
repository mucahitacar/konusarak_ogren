@model IEnumerable<KonusarakOgren.Models.exams_model>

@{
    ViewData["Title"] = "Exams";
}

<h1>Exams</h1>

<p>
    <a asp-action="Create">Create Exam</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Titles
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.baslik)
            </td>
            <td>
                @Html.ActionLink("Solve", "Sinav", new { baslik=item.baslik }) |
                @Html.ActionLink("Delete", "Delete", new { baslik=item.baslik })
            </td>
        </tr>
}
    </tbody>
</table>
